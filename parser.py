import sys
import lexer


class Parser:
    def __init__(self,lexer):
        self.lexer = lexer
        self.token = None
        self.next()
    def tokenType(token):
        return token.ttype

    def nextToken()
        self.token = lexer.build()
